

const ButtonTitle = ({buttonStyle}) => {
    return (
        <div>
            <button className="btn bg-gradient-to-r from-[#FF8938] to-[#F00] text-white border-[#FF3811]">{buttonStyle}</button>
        </div>
    );
};

export default ButtonTitle;