import { useLoaderData } from "react-router-dom";


const AllStories = () => {
    const data = useLoaderData()
    console.log(data);
    return (
        <div>
            
        </div>
    );
};

export default AllStories;